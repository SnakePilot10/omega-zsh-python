# 🐍 Omega-ZSH (Python Edition)

> **The Ultimate Shell Manager for Linux & Android (Termux)**
> *Refactorización moderna basada en Python 3.10+ y Textual TUI.*

Omega-ZSH es un gestor de configuración de entorno inteligente. Detecta tu sistema operativo (Debian, Arch, Fedora, Termux), instala las herramientas esenciales (`zsh`, `git`, `fzf`, `eza`, `bat`, etc.) y genera una configuración `.zshrc` robusta y estéticamente perfecta.

## 🚀 Instalación Rápida

No necesitas instalar dependencias manualmente. El script `install.sh` se encarga de todo (detectar Python, crear entorno virtual, instalar librerías).

```bash
# 1. Clonar o descargar este repositorio
git clone https://github.com/SnakePilot10/omega-zsh-python.git
cd omega-zsh-python

# 2. Ejecutar instalador
chmod +x install.sh
./install.sh
```

## ✨ Características

*   **Multi-Plataforma Real:** Funciona idéntico en tu PC (Ubuntu/Arch) y en tu móvil (Termux).
*   **Interfaz Visual (TUI):** Olvídate de editar archivos de texto a ciegas. Usa una interfaz moderna con soporte para ratón.
*   **Gestión de Plugins:** Activa/Desactiva plugins populares de Oh-My-Zsh y externos (autosuggestions, syntax-highlighting, etc.) con un clic.
*   **Temas Visuales:** Previsualiza y selecciona temas (incluyendo Powerlevel10k y temas personalizados).
*   **Arquitectura Segura:**
    *   **Escritura Atómica:** Nunca corromperá tu `.zshrc`. Siempre crea un backup (`.zshrc.bak.timestamp`) antes de guardar.
    *   **Personalización Aislada:** Tus alias y keys viven en archivos separados que Omega-ZSH respeta y protege.

## 🛠️ Guía de Personalización

Omega-ZSH utiliza un sistema de **3 Capas** para mantener tu configuración limpia:

1.  **`~/.zshrc` (Núcleo):** 
    *   Generado automáticamente por la App. **NO LO EDITES**, se sobrescribirá cada vez que uses la herramienta.
    
2.  **`~/.omega-zsh/personal.zsh` (Capa Estructurada):**
    *   Configurado desde la App (próximamente).
    *   Ideal para exportar rutas (`PATH`) de forma segura y definir variables de entorno estáticas.

3.  **`~/.omega-zsh/custom.zsh` (Capa Manual):**
    *   **¡Este es tu territorio!** Edítalo libremente con `nano`.
    *   Agrega aquí tus funciones complejas, alias locos o scripts de inicio.
    *   Omega-ZSH garantiza que este archivo se cargue al final de todo.

## 🖥️ Uso de la Interfaz

*   **Navegación:** Usa `Tab` o las Flechas para moverte.
*   **Atajos de Teclado:**
    *   `d`: Ir al **Dashboard** (Inicio).
    *   `p`: Ir a **Plugins**.
    *   `t`: Ir a **Temas**.
    *   `i`: **INSTALAR** cambios (Ejecuta la configuración).
    *   `q`: Salir.
*   **Ratón:** Puedes hacer clic en las pestañas y botones.

## 🔧 Requisitos Técnicos

*   **Python 3.10** o superior.
*   **Internet** (para descargar plugins la primera vez).
*   **Nerd Font** (Recomendada para ver los iconos correctamente. En Termux, la app intenta instalarla automáticamente).

## 🧑‍💻 Desarrollo

Si quieres contribuir o modificar el código:

1.  El proyecto ignora la carpeta `.venv/` y los archivos compilados (`__pycache__`).
2.  `install.sh` crea automáticamente el entorno virtual si no existe.
3.  Para ejecutar sin instalar (dev mode):
    ```bash
    ./install.sh  # Esto asegura que el venv esté creado y dependencias instaladas
    source .venv/bin/activate
    python main.py
    ```

---
*Generated by Gemini Elite Architect Module*
