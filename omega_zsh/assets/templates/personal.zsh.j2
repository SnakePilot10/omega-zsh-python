# --- OMEGA-ZSH PERSONAL CONFIGURATION ---
# Este archivo está diseñado para ser persistente y seguro.
# Puedes agregar tus keys, alias y rutas aquí.

# 1. SEGURIDAD DE RUTA (PATH)
# Agrega directorios al PATH solo si existen para evitar errores de comandos.
{% for path in extra_paths %}
if [ -d "{{ path }}" ]; then
    export PATH="{{ path }}:$PATH"
fi
{% endfor %}

# 2. VARIABLES DE ENTORNO Y KEYS
# Útil para API_KEYS, variables de desarrollo, etc.
{% for key, value in env_vars.items() %}
export {{ key }}="{{ value }}"
{% endfor %}

# 3. ALIAS PERSONALES
# Atajos rápidos para tus comandos frecuentes.
alias zr='source ~/.zshrc'                   # Desc: Recargar configuración
alias zc='nano ~/.zshrc'                    # Desc: Editar Core (.zshrc)
alias zp='nano ~/.omega-zsh/personal.zsh'   # Desc: Editar Infraestructura
alias zx='nano ~/.omega-zsh/custom.zsh'     # Desc: Editar Workflow/Custom
{% for name, cmd in aliases.items() %}
alias {{ name }}='{{ cmd }}' # Desc: Alias generado dinámicamente
{% endfor %}

# --- 4. DASHBOARD INTELIGENTE (NATIVO) ---
# Ejecutado por oz_tool.py (Python)
oz --banner



# 4. BINARIOS PROPIOS
# Si tienes scripts en carpetas locales, asegúrate de darles prioridad.
if [ -d "$HOME/.local/bin" ]; then
    export PATH="$HOME/.local/bin:$PATH"
fi

# 5. FUNCIONES DE USUARIO
# Agrega aquí lógica de shell más compleja.
function up() {
    local d=""
    limit=$1
    for ((i=1 ; i <= limit ; i++))
    do
        d=$d"../"
    done
    cd $d
}

# --- FIN DE CONFIGURACIÓN PERSONAL ---
